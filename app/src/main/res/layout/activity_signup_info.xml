<LinearLayout android:id="@+id/content_activity_signup_info"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/signupBackground"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.zaen.testly.auth.activities.SignupInfoActivity">

    <TextView
        android:id="@+id/signup_info_heading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/small_spacing"
        android:layout_gravity="center"
        android:text="@string/signup_info_heading"
        android:textColor="@color/signupText"
        android:textSize="@dimen/medium_text" />

    <include layout="@layout/form_signup_userinfo"/>

    <Button
        android:id="@+id/signup_info_signup_button"
        style="?android:textAppearanceSmall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@drawable/button_signup"
        android:foreground="@drawable/ripple_grey"
        android:text="@string/action_sign_up"
        android:onClick="onSignUp"
        android:textStyle="bold" />

</LinearLayout>